/*import React, { useState } from 'react';
import {
  KeyboardAvoidingView,
  Text,
  TextInput,
  Platform,
  StyleSheet,
  ActivityIndicator,
  View,
  Image,
  TouchableOpacity,
  ScrollView,
  Alert,
  InteractionManager,
} from 'react-native';
import { Colors, Typography } from '../../constants/theme';
import { useFonts, Baloo2_700Bold } from '@expo-google-fonts/baloo-2';
import { Nunito_400Regular, Nunito_700Bold, Nunito_300Light } from '@expo-google-fonts/nunito';
import { Ionicons } from '@expo/vector-icons';
import { Button } from '../../components/ui/Button';
import { loginSchema } from '@/schemas/authScheama';
import { router } from 'expo-router';
import { simularInicioSesion } from '../../services/authMock';


interface PublicarAlertaProps{ }

export default function PublicarAlerta({ }: PublicarAlertaProps) {
   const [fontsLoaded] = useFonts({
      Baloo2_700Bold,
      Nunito_400Regular,
      Nunito_700Bold,
      Nunito_300Light,
   });
  const [isLost, setLost] = useState(true);
  const [isFind, setFind] = useState(false);
  const [nombre, setNombre] = useState('');
  const [raza, setRaza] = useState('');
  const [ubicacion, set]

}
*/
